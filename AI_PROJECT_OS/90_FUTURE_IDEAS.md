# 90 · Future Ideas (backlog — does not pollute active work)

- Connect real data sources per business (Move Mi email first, then Slack/socials/CRM/payments).
- Full UI app (Next.js) with the Mission Control + all tabs on live data; founder-capacity "protect mode".
- services/orchestrator scheduled cadences + the continuous-improvement closed loop (auto-SOPs/templates).
- AEO / AI-search visibility live-research feeder (Market Intel); Voice-of-Customer ingestion.
- Multi-user (switch to `jwks` auth) → enterprise multi-tenant + SSO + licensing (Release 9).
- Self-optimizing organization: execution-scoring-driven agent promote/retrain/retire within gates (R10).
- The broader venture ecosystem (Oralia, Crowning Academy, Universal Aesthetic Awards, A3 portals,
  Strata HQ, etc.) onboarded onto the proven runtime as configuration, not rebuilds.
- This `/AI_PROJECT_OS` template extracted as a reusable starter for every future project.

Promote an idea into `12_TASK_QUEUE.md` only when it becomes active work.
