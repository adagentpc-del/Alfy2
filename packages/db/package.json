{
  "name": "@alfy2/db",
  "version": "0.0.0",
  "private": true,
  "description": "Postgres persistence adapters (Supabase). Implements core repository ports over a tenant-scoped connection that sets the app.tenant_id GUC so RLS enforces isolation.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@alfy2/shared": "workspace:*",
    "@alfy2/core": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  },
  "scripts": {
    "typecheck": "tsc -b"
  }
}
